// P5JS Preload
function preload() {
}
// P5JS Setup
function setup() {
  createCanvas(windowWidth, windowHeight);
}
// P5JS Draw
function draw() {
  background(255);
}
// P5JS WindowResized
function windowResized() {
  resizeCanvas(windowWidth, windowHeight);
}
